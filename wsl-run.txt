# Paste into WSL to start Beat timeline editing app
# Update REPO_ROOT to your local clone path.
REPO_ROOT="/mnt/<drive>/coding-projects/Beat timeline editing app"
cd "$REPO_ROOT/web"
fuser -k 4178/tcp || true
# first time only: npm install
npm run dev -- --host --port 4178
